interface:
  display_name: "Idea to Camera-Ready Talk"
  short_description: "Turn an idea into a spoken script and cue cards"
  default_prompt: "Use $idea-to-camera-ready-talk to turn this idea into a spoken script I can deliver naturally, then compress it into recording cue cards."
policy:
  allow_implicit_invocation: true
